{
  "name": "@baseline/delivery-api",
  "version": "1.0.0",
  "private": true,
  "description": "Delivery's service: owns projects, the work breakdown and allocations. Zero dependencies.",
  "type": "module",
  "scripts": {
    "start": "node src/server.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": { "@baseline/contracts": "1.0.0" },
  "devDependencies": { "@types/node": "^22.10.0", "typescript": "^5.6.3" }
}
